{{ form_start(form) }}
    <div class="card-body">
        {{ form_widget(form) }}
    </div>

    <div class="card-footer">
        <button type="submit" class="btn btn-primary">{{ button_label|default('Sumbit report') }}</button>
    </div>
{{ form_end(form) }}
