{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block title %}Blocker report{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-center" style="padding: 10px;">
        <div class="col-md-6">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Create a new blocker report</h3>
                </div>
                {{ include('activity/_form.html.twig') }}
            </div>
            <a href="{{ path('home') }}">Cancel request</a>
        </div>
    </div>
{% endblock %}
